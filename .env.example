# MongoDB connection string used by db/connectDB.js on startup.
# Point to a running MongoDB instance before `npm run dev`.
MONGODB_URI=mongodb://localhost:27017/crudapp

# Optional: override the default HTTP port (defaults to 3000)
PORT=3000
